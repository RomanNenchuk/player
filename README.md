# TED Clone

## How to run
1. **Enable Developer Mode**: Press `Home(x3), Up(x2), Right, Left, Right, Left, Right` on your remote. Set a password.
2. **Configure**: Add `ROKU_IP` and `ROKU_PASSWORD` to your `.env` file.
3. **Deploy**: Run
* ```chmod +x deploy.sh```
* ```./deploy.sh```